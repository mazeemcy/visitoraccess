# visitoraccess

Windows users: a helper script to install prerequisites and dependencies is available at `visitoraccess/scripts/install-prereqs-windows.bat`. Run it as Administrator to set up Python, Node.js, Docker (optional), Rust (optional), and project dependencies.

